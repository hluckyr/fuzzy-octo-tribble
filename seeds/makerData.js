const { Maker } = require('../paddles');

const makerdata = [
  {
    name: 'Gearbox',
  },
  {
    name: 'Joola',
  },
];

const seedMaker = () => Maker.bulkCreate(makerata);

module.exports = seedMaker;
